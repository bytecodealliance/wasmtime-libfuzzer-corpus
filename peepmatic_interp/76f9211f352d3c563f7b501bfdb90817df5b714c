;;onond $C $x))
(=> (when (isub $C $x)
      (fits-in-native-word $C))
    (irsub_imm $x))

;; Unwhose  $x))
(=> (when (isub $C $x)
     (fits-in-native-word $C))
    (irsub_imm $C $x))

;; Unions whose  $x))
(=> (when (isub $C $x) (fits-in-native-word $C))
    (irsub_imm _im$Con
