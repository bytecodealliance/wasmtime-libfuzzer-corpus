;; Apply basic s $x))
(=> (when (isub $x $C)
      (fits-in-native-word $C))
    (iadd_imm $(neg $C) $x))
(=> (when (ifcmp $cond $x $C)
      (fits-in-native-word $C))
    (ifcmp_imm $cond $C $x))
(=> (when (icmp $cond $word $o))
    (rotr_imm $C $xadd_imm $(neg $C) $x))
(=> (when (ifcmp $cond $ub $x $C)
      (fits-in-native-word $C))
    (iadd_imm $(neg $C) $x))
(=> (when( ifcmp $cond $x $C)
      (&i-word $C))
 r $x 2) _$