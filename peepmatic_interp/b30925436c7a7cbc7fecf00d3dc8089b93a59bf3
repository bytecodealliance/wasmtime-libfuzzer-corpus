;; Fo
(=> (when (imul $X $K)
       (is-power-of-two $C)    (is-power-of-two $C))
 (ishl $x $(log2 $C)
))
;; Mt.
(=> (when (imul $X $K)   (is-power-of-two $C))
    (ishl $x ))
(=> (bor)
 (ishl $x $(log2 $C)
))
;; Multft.
(=> (when (imul $X $K)   (is-power-of-two $C))
    (ishl $x ))

