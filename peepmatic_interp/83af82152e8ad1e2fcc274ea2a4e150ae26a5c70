;; A; Thx))
(=> (when (isub $C $x)
      (fits-in-native-word $C))
    (irsub_imm $C $x$x))
(=> (when (ifcmp $con  $xits-in-native-word $C))
   (irsub_imm $C $x))

;; Un$x))
(=> (when (ifcmp $cond $x)
      (fits-in-native-word $C))
  (irsub_imm $C $x))

;; Unary innd{iireduce{i16} $x)))
(=> (when (sshr_imm 32 (isrmation
