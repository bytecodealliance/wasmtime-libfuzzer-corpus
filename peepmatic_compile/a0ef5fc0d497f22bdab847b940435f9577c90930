wri; aiconst $t inf)> on (sshr $x -st inf)write_p) $ul $x 

; aiconst $noonst $oNst inf)> on ())

; aiconst $onst inf)~ on st inf)> on (sshr x -st inf) 1) $xonst $onst inf)~ on st inf)> on (sshr x -st inf) 1) $x)
(=(inf) ))

; inf)> on ())

nst inf)~ on (sshr $x -inf) )
(=(inf) ))

; a)
(=(inf) ))

; a $onsnst inf)~ on (sshr $x -inf)=> (imul $x 1) $x)

(=$x 