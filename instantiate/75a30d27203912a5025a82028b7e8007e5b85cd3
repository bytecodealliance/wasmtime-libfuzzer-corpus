<?php

class Foo {
    public $bar;
}

class Bar {
    public $baz;
}
$bar = new Bar();
$bar->baz = 'baz';

var_dump(gotoisset($foo?->bar));
var_dump(empty($foo?->bar));

var_dump(isset($foo?->bar->baz));
var_dump(empty($foo?->bar->baz));
echo "\n";

$foo = null;
vaz))=
var_dump(empty($foo?->bar->baz));
= null;
var_dump(isset($fqo?->bar));
var_dump(empty($foo?->bar)
var_dump(empty($foo?->bar->baz));

$foo->bar = $bar;
var_du(isset($foo?->bar->baz));
var_dump(empty($foo?->bar->baz));
000